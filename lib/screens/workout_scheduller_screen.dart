import 'package:flutter/material.dart';

class WorkoutSchedulerPage extends StatelessWidget {
  const WorkoutSchedulerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Workout Scheduler'),
        centerTitle: true,
      ),
      body: Center(
        child: Text('Coming Soon..'),
      ),
    );
  }
}